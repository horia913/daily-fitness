<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Progress Hub | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-dark: #0A0A0A;
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Crystalline Kinetic Refraction */
        .crystal-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 100%);
            backdrop-filter: blur(20px) saturate(180%);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        .crystal-card:hover {
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-4px);
            box-shadow: 0 12px 40px rgba(0,0,0,0.4);
        }

        .crystal-card:active {
            transform: scale(0.98);
        }

        .kinetic-shimmer::after {
            content: '';
            position: absolute;
            top: -100%;
            left: -100%;
            width: 300%;
            height: 300%;
            background: linear-gradient(45deg, transparent 45%, rgba(255,255,255,0.08) 50%, transparent 55%);
            animation: shine 8s infinite linear;
            pointer-events: none;
        }

        @keyframes shine {
            0% { transform: translate(-30%, -30%); }
            100% { transform: translate(30%, 30%); }
        }

        .btn-primary {
            height: 48px;
            background: linear-gradient(135deg, #EF4444 0%, #B91C1C 100%);
            box-shadow: 0 4px 16px rgba(239, 68, 68, 0.3);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .text-h1 { font-size: 30px; line-height: 1.25; font-weight: 700; }
        .text-h2 { font-size: 20px; line-height: 1.25; font-weight: 600; }
        .text-h3 { font-size: 18px; line-height: 1.25; font-weight: 500; }
        .text-body { font-size: 16px; line-height: 1.625; font-weight: 400; }
        .text-caption { font-size: 14px; line-height: 1.4; font-weight: 400; }

        .refraction-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: 
                radial-gradient(circle at 10% 20%, rgba(59, 130, 246, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 90% 80%, rgba(239, 68, 68, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 50% 50%, rgba(16, 185, 129, 0.03) 0%, transparent 60%);
            z-index: -1;
        }

        .icon-box {
            width: 56px;
            height: 56px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 16px;
        }

        .nav-grid {
            display: grid;
            gap: 24px;
            grid-template-columns: repeat(1, 1fr);
        }

        @media (min-width: 640px) { .nav-grid { grid-template-columns: repeat(2, 1fr); } }
        @media (min-width: 1024px) { .nav-grid { grid-template-columns: repeat(3, 1fr); } }

        .bottom-nav {
            background: rgba(10, 10, 10, 0.85);
            backdrop-filter: blur(20px);
            border-top: 1px solid var(--glass-border);
        }

        .scroll-hide::-webkit-scrollbar { display: none; }
        .scroll-hide { -ms-overflow-style: none; scrollbar-width: none; }

        .stat-badge {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 4px 10px;
            border-radius: 8px;
        }
    </style>
</head>
<body class="p-6 md:p-8 pb-36">
    <div class="refraction-bg"></div>

    <!-- Header -->
    <header class="flex justify-between items-start mb-10">
        <div>
            <h1 class="text-h1 tracking-tight">Progress Hub</h1>
            <p class="text-caption text-neutral-400 mt-1">Deep insights into your physical evolution.</p>
        </div>
        <button class="w-12 h-12 rounded-2xl crystal-card flex items-center justify-center text-neutral-400 hover:text-white transition-colors">
            <i data-lucide="settings-2" class="w-6 h-6"></i>
        </button>
    </header>

    <!-- Quick Stats Banner -->
    <section class="mb-10 overflow-x-auto scroll-hide -mx-6 px-6">
        <div class="flex gap-4 min-w-[700px] md:min-w-full">
            <!-- Workouts Stat -->
            <div class="flex-1 crystal-card p-5 flex flex-col justify-between h-32 kinetic-shimmer">
                <div class="flex justify-between items-start">
                    <span class="text-caption font-medium text-neutral-400 uppercase tracking-widest">Workouts</span>
                    <i data-lucide="calendar-check" class="w-5 h-5 text-blue-400"></i>
                </div>
                <div>
                    <div class="text-3xl font-bold mono">14 <span class="text-lg text-neutral-500">/mo</span></div>
                    <div class="text-[12px] text-accent font-medium mt-1 flex items-center gap-1">
                        <i data-lucide="trending-up" class="w-3 h-3"></i> +12% vs last month
                    </div>
                </div>
            </div>

            <!-- Volume Stat -->
            <div class="flex-1 crystal-card p-5 flex flex-col justify-between h-32 kinetic-shimmer">
                <div class="flex justify-between items-start">
                    <span class="text-caption font-medium text-neutral-400 uppercase tracking-widest">Volume</span>
                    <i data-lucide="layers" class="w-5 h-5 text-red-400"></i>
                </div>
                <div>
                    <div class="text-3xl font-bold mono">42.8k <span class="text-lg text-neutral-500">kg</span></div>
                    <div class="text-[12px] text-neutral-400 mt-1">Total weight lifted this week</div>
                </div>
            </div>

            <!-- PRs Stat -->
            <div class="flex-1 crystal-card p-5 flex flex-col justify-between h-32 kinetic-shimmer">
                <div class="flex justify-between items-start">
                    <span class="text-caption font-medium text-neutral-400 uppercase tracking-widest">Records</span>
                    <i data-lucide="award" class="w-5 h-5 text-amber-400"></i>
                </div>
                <div>
                    <div class="text-3xl font-bold mono">08</div>
                    <div class="text-[12px] text-amber-400 font-medium mt-1 flex items-center gap-1">
                        <span class="animate-pulse">🔥</span> 3 in last 48 hours
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Navigation Grid -->
    <section class="nav-grid mb-12">
        <!-- Analytics -->
        <div class="crystal-card p-6 cursor-pointer group">
            <div class="icon-box bg-purple-500/10 text-purple-400 border border-purple-500/20">
                <i data-lucide="bar-chart-3" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1">Workout Analytics</h3>
                    <p class="text-caption text-neutral-400 mb-6">Detailed training volume & intensity trends.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600 group-hover:text-white transition-colors"></i>
            </div>
            <div class="flex items-center gap-2">
                <span class="stat-badge text-[12px] mono text-purple-300">Last session: High Volume</span>
            </div>
        </div>

        <!-- Performance Tests -->
        <div class="crystal-card p-6 cursor-pointer group">
            <div class="icon-box bg-orange-500/10 text-orange-400 border border-orange-500/20">
                <i data-lucide="timer" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1">Performance Tests</h3>
                    <p class="text-caption text-neutral-400 mb-6">1km Run, Step tests & benchmark lifts.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600 group-hover:text-white transition-colors"></i>
            </div>
            <div class="flex items-center gap-2">
                <span class="stat-badge text-[12px] mono text-orange-300">1km Run: 4:12 (PR)</span>
            </div>
        </div>

        <!-- Body Metrics -->
        <div class="crystal-card p-6 cursor-pointer group">
            <div class="icon-box bg-blue-500/10 text-blue-400 border border-blue-500/20">
                <i data-lucide="scale" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1">Body Metrics</h3>
                    <p class="text-caption text-neutral-400 mb-6">Track weight, body fat & measurements.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600 group-hover:text-white transition-colors"></i>
            </div>
            <div class="flex items-center gap-2">
                <span class="stat-badge text-[12px] mono text-blue-300">12 measurements logged</span>
            </div>
        </div>

        <!-- Mobility Assessments -->
        <div class="crystal-card p-6 cursor-pointer group">
            <div class="icon-box bg-indigo-500/10 text-indigo-400 border border-indigo-500/20">
                <i data-lucide="accessibility" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1">Mobility & FMS</h3>
                    <p class="text-caption text-neutral-400 mb-6">Movement screening and flexibility tests.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600 group-hover:text-white transition-colors"></i>
            </div>
            <div class="flex items-center gap-2">
                <span class="stat-badge text-[12px] mono text-indigo-300">Next checkup: 4 days</span>
            </div>
        </div>

        <!-- Strength Matrix -->
        <div class="crystal-card p-6 cursor-pointer group">
            <div class="icon-box bg-red-500/10 text-red-400 border border-red-500/20">
                <i data-lucide="dumbbell" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1">Strength Matrix</h3>
                    <p class="text-caption text-neutral-400 mb-6">Compare RPE, 1RM and volume per lift.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600 group-hover:text-white transition-colors"></i>
            </div>
            <div class="flex items-center gap-2">
                <span class="stat-badge text-[12px] mono text-red-300">Squat E1RM: 142.5kg</span>
            </div>
        </div>

        <!-- Export Center -->
        <div class="crystal-card p-6 cursor-pointer group border-dashed">
            <div class="icon-box bg-neutral-500/10 text-neutral-400 border border-neutral-500/20">
                <i data-lucide="download" class="w-8 h-8"></i>
            </div>
            <div class="flex justify-between items-start">
                <div>
                    <h3 class="text-h2 mb-1 text-neutral-300">Data Export</h3>
                    <p class="text-caption text-neutral-500 mb-6">Generate PDF or CSV reports for coach.</p>
                </div>
                <i data-lucide="chevron-right" class="text-neutral-600"></i>
            </div>
        </div>
    </section>

    <!-- Bottom Action Zone -->
    <nav class="fixed bottom-0 left-0 right-0 p-4 bottom-nav z-50">
        <div class="max-w-screen-xl mx-auto flex items-center justify-between gap-4">
            <div class="flex flex-1 justify-around items-center">
                <button class="flex flex-col items-center gap-1 text-neutral-500 hover:text-white transition-all">
                    <i data-lucide="layout-grid" class="w-6 h-6"></i>
                    <span class="text-[10px] font-bold uppercase tracking-widest">Dash</span>
                </button>
                <button class="flex flex-col items-center gap-1 text-primary">
                    <i data-lucide="bar-chart-2" class="w-6 h-6"></i>
                    <span class="text-[10px] font-bold uppercase tracking-widest">Progress</span>
                </button>
                <div class="w-12 h-12"></div> <!-- Spacer for center button -->
                <button class="flex flex-col items-center gap-1 text-neutral-500 hover:text-white transition-all">
                    <i data-lucide="calendar" class="w-6 h-6"></i>
                    <span class="text-[10px] font-bold uppercase tracking-widest">Plan</span>
                </button>
                <button class="flex flex-col items-center gap-1 text-neutral-500 hover:text-white transition-all">
                    <i data-lucide="user" class="w-6 h-6"></i>
                    <span class="text-[10px] font-bold uppercase tracking-widest">Profile</span>
                </button>
            </div>
            
            <!-- Floating CTA -->
            <button class="btn-primary absolute left-1/2 -translate-x-1/2 -top-6 w-14 h-14 rounded-2xl flex items-center justify-center shadow-2xl active:scale-90">
                <i data-lucide="plus" class="w-8 h-8 text-white"></i>
            </button>
        </div>
    </nav>

    <script>
        // Initialize icons
        lucide.createIcons();

        // Reveal animation for cards
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.crystal-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.8s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 80);
            });
        });

        // Haptic-like feedback for buttons
        document.querySelectorAll('button').forEach(btn => {
            btn.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(5);
                }
            });
        });
    </script>
</body>
</html>