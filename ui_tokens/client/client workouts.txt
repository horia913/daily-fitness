<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Protocol | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-dark: #070708;
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.08);
            --quartz-refraction: linear-gradient(135deg, rgba(239, 68, 68, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
            line-height: 1.625;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Faceted Quartz Surfaces */
        .quartz-card {
            background: linear-gradient(165deg, rgba(255,255,255,0.06) 0%, rgba(255,255,255,0.01) 100%);
            backdrop-filter: blur(24px) saturate(180%);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        .quartz-card::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent 45%, rgba(255,255,255,0.05) 50%, transparent 55%);
            background-size: 200% 200%;
            pointer-events: none;
            transition: 0.6s;
        }

        .quartz-card:hover::before {
            background-position: 100% 100%;
        }

        .kinetic-shimmer {
            position: relative;
            overflow: hidden;
        }

        .kinetic-shimmer::after {
            content: '';
            position: absolute;
            top: -150%;
            left: -150%;
            width: 400%;
            height: 400%;
            background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, transparent 80%);
            animation: rotate-shimmer 12s infinite linear;
            pointer-events: none;
        }

        @keyframes rotate-shimmer {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Typography Hierarchy */
        .text-h1 { font-size: 30px; font-weight: 700; line-height: 1.25; letter-spacing: -0.02em; }
        .text-h2 { font-size: 20px; font-weight: 600; line-height: 1.25; }
        .text-h3 { font-size: 18px; font-weight: 500; line-height: 1.25; }
        .text-body { font-size: 16px; font-weight: 400; }
        .text-caption { font-size: 14px; font-weight: 400; }

        /* Interaction States */
        .btn-primary {
            height: 56px;
            background: linear-gradient(135deg, #10B981 0%, #059669 100%);
            box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);
            border-radius: 18px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
        }

        .btn-primary:active { transform: scale(0.96); filter: brightness(0.9); }
        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(16, 185, 129, 0.4); }

        .btn-secondary {
            height: 48px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            transition: all 0.2s ease;
        }

        .btn-secondary:hover { background: rgba(255, 255, 255, 0.1); border-color: rgba(255, 255, 255, 0.2); }

        /* Specialized Components */
        .status-badge {
            padding: 4px 12px;
            border-radius: 99px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }

        .badge-today { background: linear-gradient(90deg, #EF4444, #F59E0B); animation: pulse-red 2s infinite; }
        @keyframes pulse-red {
            0% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4); }
            70% { box-shadow: 0 0 0 10px rgba(239, 68, 68, 0); }
            100% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0); }
        }

        .progress-bar-container {
            height: 8px;
            background: rgba(255,255,255,0.05);
            border-radius: 4px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--secondary), var(--accent));
            border-radius: 4px;
            transition: width 1s cubic-bezier(0.65, 0, 0.35, 1);
        }

        .quartz-border-bottom {
            border-bottom: 1px solid var(--glass-border);
        }

        /* Navigation Glass */
        .bottom-nav {
            background: rgba(7, 7, 8, 0.85);
            backdrop-filter: blur(20px);
            border-top: 1px solid var(--glass-border);
        }

        .refraction-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: 
                radial-gradient(circle at 0% 0%, rgba(239, 68, 68, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 100% 100%, rgba(59, 130, 246, 0.08) 0%, transparent 40%);
            z-index: -1;
        }

        .scroll-hide::-webkit-scrollbar { display: none; }
    </style>
</head>
<body class="p-6 md:p-10 pb-36">
    <div class="refraction-bg"></div>

    <!-- Header Section -->
    <header class="max-w-4xl mx-auto flex justify-between items-center mb-10">
        <div>
            <h1 class="text-h1">Your Workouts</h1>
            <p class="text-caption text-neutral-400 mt-1">Refining the physique • Week 4</p>
        </div>
        <div class="w-14 h-14 rounded-2xl quartz-card flex items-center justify-center p-1 border-2 border-white/5">
            <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=Alex&backgroundColor=0A0A0A" alt="Avatar" class="w-full h-full rounded-xl object-cover">
        </div>
    </header>

    <main class="max-w-4xl mx-auto space-y-8">
        
        <!-- Today's Hero Card -->
        <section class="animate-in fade-in slide-in-from-bottom-4 duration-700">
            <div class="quartz-card kinetic-shimmer p-8 min-h-[280px] flex flex-col justify-between border-l-4 border-l-primary">
                <div class="flex justify-between items-start">
                    <div class="space-y-3">
                        <span class="status-badge badge-today text-white">
                            <i data-lucide="zap" class="w-3 h-3 fill-current"></i> Today
                        </span>
                        <h2 class="text-3xl font-bold tracking-tight">Push Protocol: Hypertrophy</h2>
                        <div class="flex flex-wrap gap-5 text-neutral-300">
                            <div class="flex items-center gap-2">
                                <i data-lucide="dumbbell" class="w-5 h-5 text-primary"></i>
                                <span class="text-body font-medium">6 Exercises</span>
                            </div>
                            <div class="flex items-center gap-2">
                                <i data-lucide="layers" class="w-5 h-5 text-primary"></i>
                                <span class="text-body font-medium">18 Sets</span>
                            </div>
                            <div class="flex items-center gap-2">
                                <i data-lucide="clock" class="w-5 h-5 text-neutral-500"></i>
                                <span class="text-body font-medium mono">~55 min</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-8 flex flex-col md:flex-row gap-4">
                    <button class="btn-primary flex-1 flex items-center justify-center gap-3 text-lg font-bold">
                        <i data-lucide="play" class="w-6 h-6 fill-current"></i>
                        Start Workout
                    </button>
                    <button class="btn-secondary px-6 flex items-center justify-center gap-2 text-neutral-300 font-semibold">
                        <i data-lucide="info" class="w-5 h-5"></i>
                        Details
                    </button>
                </div>
            </div>
        </section>

        <!-- Weekly Progress Banner -->
        <section class="quartz-card p-6 border-l-4 border-l-secondary">
            <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
                <div class="flex-1 space-y-2">
                    <div class="flex justify-between items-end">
                        <h3 class="text-h3">Weekly Progress</h3>
                        <span class="mono text-secondary font-bold">3 / 4 <span class="text-neutral-500 font-normal">Workouts</span></span>
                    </div>
                    <div class="progress-bar-container">
                        <div class="progress-fill" style="width: 75%"></div>
                    </div>
                </div>
                <div class="h-12 w-px bg-white/10 hidden md:block"></div>
                <div class="flex gap-8">
                    <div>
                        <p class="text-caption text-neutral-400 uppercase tracking-widest mb-1">Total Volume</p>
                        <p class="text-xl font-bold mono tracking-tighter">14,250<span class="text-neutral-500 ml-1 text-sm font-normal">kg</span></p>
                    </div>
                    <div>
                        <p class="text-caption text-neutral-400 uppercase tracking-widest mb-1">Active Time</p>
                        <p class="text-xl font-bold mono tracking-tighter">184<span class="text-neutral-500 ml-1 text-sm font-normal">min</span></p>
                    </div>
                </div>
            </div>
        </section>

        <!-- This Week's Workouts -->
        <section class="space-y-4">
            <h3 class="text-h2 px-1">This Week</h3>
            <div class="space-y-3">
                
                <!-- Workout Item: Completed -->
                <div class="quartz-card p-5 flex items-center justify-between group cursor-pointer hover:bg-white/[0.04]">
                    <div class="flex items-center gap-5">
                        <div class="w-14 h-14 rounded-xl bg-accent/10 border border-accent/20 flex flex-col items-center justify-center text-accent">
                            <span class="text-xs font-bold uppercase mono">Jan</span>
                            <span class="text-xl font-bold">12</span>
                        </div>
                        <div>
                            <h4 class="text-h3 font-semibold group-hover:text-accent transition-colors">Leg Day: Chain Focus</h4>
                            <p class="text-caption text-neutral-400 mt-1">Completed at 08:45 AM • 62 min</p>
                        </div>
                    </div>
                    <div class="flex items-center gap-4">
                        <div class="status-badge bg-accent/20 text-accent border border-accent/30">
                            <i data-lucide="check-circle" class="w-3.5 h-3.5"></i> DONE
                        </div>
                        <i data-lucide="chevron-right" class="w-6 h-6 text-neutral-600 group-hover:text-white transition-colors"></i>
                    </div>
                </div>

                <!-- Workout Item: Scheduled -->
                <div class="quartz-card p-5 flex items-center justify-between group cursor-pointer hover:bg-white/[0.04]">
                    <div class="flex items-center gap-5">
                        <div class="w-14 h-14 rounded-xl bg-secondary/10 border border-secondary/20 flex flex-col items-center justify-center text-secondary">
                            <span class="text-xs font-bold uppercase mono">Jan</span>
                            <span class="text-xl font-bold">14</span>
                        </div>
                        <div>
                            <h4 class="text-h3 font-semibold group-hover:text-secondary transition-colors">Pull Day: Back & Rear Delts</h4>
                            <p class="text-caption text-neutral-400 mt-1">Scheduled • 5 Exercises</p>
                        </div>
                    </div>
                    <div class="flex items-center gap-4">
                        <div class="status-badge bg-secondary/20 text-secondary border border-secondary/30">
                             UPCOMING
                        </div>
                        <i data-lucide="chevron-right" class="w-6 h-6 text-neutral-600 group-hover:text-white transition-colors"></i>
                    </div>
                </div>

                <!-- Workout Item: Skipped -->
                <div class="quartz-card p-5 flex items-center justify-between group cursor-pointer hover:bg-white/[0.04] opacity-70">
                    <div class="flex items-center gap-5">
                        <div class="w-14 h-14 rounded-xl bg-neutral-800 border border-neutral-700 flex flex-col items-center justify-center text-neutral-500">
                            <span class="text-xs font-bold uppercase mono">Jan</span>
                            <span class="text-xl font-bold">10</span>
                        </div>
                        <div>
                            <h4 class="text-h3 font-semibold">Active Recovery: Mobility</h4>
                            <p class="text-caption text-neutral-500 mt-1">Marked as skipped</p>
                        </div>
                    </div>
                    <div class="flex items-center gap-4">
                        <div class="status-badge bg-neutral-800 text-neutral-500 border border-neutral-700">
                             SKIPPED
                        </div>
                        <i data-lucide="chevron-right" class="w-6 h-6 text-neutral-600 group-hover:text-white transition-colors"></i>
                    </div>
                </div>

            </div>
        </section>

        <!-- Upcoming Section (Collapsible Style) -->
        <section class="py-4">
            <button class="w-full flex justify-between items-center py-4 quartz-border-bottom group">
                <span class="text-h2 text-neutral-400 group-hover:text-white transition-colors">Upcoming: Week of Jan 15-21</span>
                <i data-lucide="chevron-down" class="w-6 h-6 text-neutral-600"></i>
            </button>
        </section>

    </main>

    <!-- Navigation Fixed Footer -->
    <nav class="fixed bottom-0 left-0 right-0 h-24 bottom-nav z-50 px-6">
        <div class="max-w-4xl mx-auto h-full flex items-center justify-between gap-4">
            <div class="flex flex-1 justify-around md:justify-start md:gap-16">
                <button class="flex flex-col items-center gap-1.5 text-neutral-500 hover:text-white transition-all group">
                    <i data-lucide="layout-grid" class="w-7 h-7 group-hover:scale-110 transition-transform"></i>
                    <span class="text-[11px] font-bold uppercase tracking-[0.15em]">Dashboard</span>
                </button>
                <button class="flex flex-col items-center gap-1.5 text-primary">
                    <i data-lucide="calendar" class="w-7 h-7 scale-110"></i>
                    <span class="text-[11px] font-bold uppercase tracking-[0.15em]">Workouts</span>
                </button>
                <button class="flex flex-col items-center gap-1.5 text-neutral-500 hover:text-white transition-all group">
                    <i data-lucide="line-chart" class="w-7 h-7 group-hover:scale-110 transition-transform"></i>
                    <span class="text-[11px] font-bold uppercase tracking-[0.15em]">Analytics</span>
                </button>
            </div>
            
            <button class="btn-primary w-16 h-16 md:w-auto md:px-8 flex items-center justify-center gap-3 shadow-2xl active:scale-90 transition-transform">
                <i data-lucide="plus" class="w-7 h-7"></i>
                <span class="hidden md:block font-bold">Quick Log</span>
            </button>
        </div>
    </nav>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Staggered entry animation for elements
        document.addEventListener('DOMContentLoaded', () => {
            const items = document.querySelectorAll('.quartz-card');
            items.forEach((item, index) => {
                item.style.opacity = '0';
                item.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    item.style.transition = 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    item.style.opacity = '1';
                    item.style.transform = 'translateY(0)';
                }, index * 80);
            });
        });

        // Simulating haptic feedback for primary interactions
        document.querySelectorAll('.btn-primary, .quartz-card').forEach(el => {
            el.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(12);
                }
            });
        });
    </script>
</body>
</html>