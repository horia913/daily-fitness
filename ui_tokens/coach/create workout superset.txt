<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configure Superset | Vitreous Carbon Tectonics</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #A855F7; /* Purple for Superset */
            --accent: #10B981;
            --bg-dark: #050505;
            --carbon-glass: linear-gradient(165deg, rgba(20, 20, 20, 0.9) 0%, rgba(5, 5, 5, 0.95) 100%);
            --tectonic-edge: rgba(168, 85, 247, 0.2);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            -webkit-font-smoothing: antialiased;
            overflow-x: hidden;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Vitreous Carbon Surfaces */
        .carbon-surface {
            background: var(--carbon-glass);
            backdrop-filter: blur(40px) saturate(150%);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            border-radius: 28px;
            position: relative;
        }

        .carbon-surface::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent 0%, rgba(168, 85, 247, 0.03) 50%, transparent 100%);
            pointer-events: none;
        }

        /* Tectonic Stack Animation */
        .tectonic-layer {
            transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
            border-left: 4px solid transparent;
        }

        .tectonic-layer:focus-within {
            transform: translateX(8px);
            border-left-color: var(--secondary);
            background: rgba(168, 85, 247, 0.04);
        }

        /* Glass Input Fields */
        .glass-input {
            height: 52px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 14px;
            padding: 0 16px;
            transition: all 0.3s ease;
            width: 100%;
            font-size: 16px;
        }

        .glass-input:focus {
            outline: none;
            border-color: var(--secondary);
            background: rgba(168, 85, 247, 0.08);
            box-shadow: 0 0 20px rgba(168, 85, 247, 0.15);
        }

        .glass-textarea {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 14px;
            padding: 16px;
            transition: all 0.3s ease;
            width: 100%;
            font-size: 16px;
        }

        /* Vertical Connection Link */
        .superset-link {
            position: absolute;
            left: 24px;
            top: 80px;
            bottom: 80px;
            width: 2px;
            background: linear-gradient(to bottom, var(--secondary), transparent, var(--secondary));
            opacity: 0.3;
        }

        /* High-Affordance Buttons */
        .btn-action {
            height: 56px;
            border-radius: 16px;
            font-weight: 600;
            letter-spacing: 0.025em;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            cursor: pointer;
        }

        .btn-primary {
            background: linear-gradient(135deg, #A855F7 0%, #7E22CE 100%);
            color: white;
            box-shadow: 0 10px 25px -5px rgba(168, 85, 247, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-2px) scale(1.02);
            box-shadow: 0 15px 30px -5px rgba(168, 85, 247, 0.5);
        }

        .btn-primary:active { transform: scale(0.97); }

        .btn-ghost {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #D1D5DB;
        }

        .btn-ghost:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        /* Refraction Background Elements */
        .refraction-bg {
            position: fixed;
            inset: 0;
            z-index: -1;
            background: 
                radial-gradient(circle at 80% 20%, rgba(168, 85, 247, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 20% 80%, rgba(239, 68, 68, 0.05) 0%, transparent 40%);
        }

        .protocol-badge {
            background: rgba(168, 85, 247, 0.15);
            color: #C084FC;
            border: 1px solid rgba(168, 85, 247, 0.3);
            padding: 4px 12px;
            border-radius: 99px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
    </style>
</head>
<body class="p-4 md:p-8 min-h-screen">
    <div class="refraction-bg"></div>

    <main class="max-w-2xl mx-auto pt-12 pb-32">
        <!-- Header Section -->
        <header class="mb-10 flex justify-between items-end">
            <div>
                <nav class="flex items-center gap-2 text-neutral-500 mb-2 mono text-sm">
                    <span>COACH</span>
                    <i data-lucide="chevron-right" class="w-3 h-3"></i>
                    <span>TEMPLATES</span>
                </nav>
                <h1 class="text-3xl font-bold tracking-tight">Configure Superset</h1>
            </div>
            <div class="protocol-badge mono">A &rarr; B Protocol</div>
        </header>

        <!-- Main Configuration Card -->
        <div class="carbon-surface p-6 md:p-8 space-y-10">
            
            <!-- Exercise A -->
            <section class="relative tectonic-layer pl-8">
                <div class="absolute left-0 top-1 w-6 h-6 rounded-full bg-secondary flex items-center justify-center text-[12px] font-bold">A</div>
                <div class="space-y-4">
                    <div class="flex justify-between items-center">
                        <label class="text-sm font-semibold text-neutral-400 uppercase tracking-widest mono">First Exercise</label>
                        <span class="text-xs text-neutral-500">REQUIRED</span>
                    </div>
                    <div class="relative">
                        <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-neutral-500"></i>
                        <input type="text" placeholder="Search first exercise..." class="glass-input pl-12" required>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div class="space-y-2">
                            <label class="text-[11px] font-bold text-neutral-500 uppercase tracking-wider mono">Reps (A)</label>
                            <input type="text" placeholder="e.g. 8-12" class="glass-input text-center font-semibold">
                        </div>
                        <div class="space-y-2">
                            <label class="text-[11px] font-bold text-neutral-500 uppercase tracking-wider mono">Load (1RM %)</label>
                            <input type="number" placeholder="75" class="glass-input text-center font-semibold">
                        </div>
                    </div>
                </div>
            </section>

            <!-- Visual Link -->
            <div class="flex justify-center py-2">
                <div class="h-8 w-px bg-secondary opacity-30 relative">
                    <i data-lucide="link" class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-4 h-4 text-secondary opacity-60"></i>
                </div>
            </div>

            <!-- Exercise B -->
            <section class="relative tectonic-layer pl-8">
                <div class="absolute left-0 top-1 w-6 h-6 rounded-full border-2 border-secondary flex items-center justify-center text-[12px] font-bold text-secondary">B</div>
                <div class="space-y-4">
                    <label class="text-sm font-semibold text-neutral-400 uppercase tracking-widest mono block">Second Exercise</label>
                    <div class="relative">
                        <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-neutral-500"></i>
                        <input type="text" placeholder="Search second exercise..." class="glass-input pl-12" required>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div class="space-y-2">
                            <label class="text-[11px] font-bold text-neutral-500 uppercase tracking-wider mono">Reps (B)</label>
                            <input type="text" placeholder="e.g. 15" class="glass-input text-center font-semibold" name="superset_reps">
                        </div>
                        <div class="space-y-2">
                            <label class="text-[11px] font-bold text-neutral-500 uppercase tracking-wider mono">Load (1RM %)</label>
                            <input type="number" placeholder="Optional" class="glass-input text-center font-semibold">
                        </div>
                    </div>
                </div>
            </section>

            <hr class="border-white/5">

            <!-- Global Superset Controls -->
            <section class="grid grid-cols-2 gap-6">
                <div class="space-y-3">
                    <label class="flex items-center gap-2 text-sm font-semibold text-neutral-300">
                        <i data-lucide="layers" class="w-4 h-4 text-secondary"></i>
                        Total Sets
                    </label>
                    <input type="number" min="1" value="3" class="glass-input text-xl font-bold text-center">
                </div>
                <div class="space-y-3">
                    <label class="flex items-center gap-2 text-sm font-semibold text-neutral-300">
                        <i data-lucide="timer" class="w-4 h-4 text-secondary"></i>
                        Rest (Sec)
                    </label>
                    <input type="number" min="0" value="90" class="glass-input text-xl font-bold text-center">
                </div>
            </section>

            <!-- Notes -->
            <section class="space-y-3">
                <label class="text-sm font-semibold text-neutral-300 block">Coaching Notes (Optional)</label>
                <textarea rows="3" placeholder="Focus on controlled eccentric on A, maximum metabolic stress on B..." class="glass-textarea"></textarea>
            </section>
        </div>

        <!-- Footer Actions -->
        <footer class="fixed bottom-0 left-0 right-0 p-6 bg-[#050505]/80 backdrop-blur-xl border-t border-white/5 z-50">
            <div class="max-w-2xl mx-auto flex gap-4">
                <button class="btn-action btn-ghost flex-1">
                    Cancel
                </button>
                <button class="btn-action btn-primary flex-[2]">
                    <i data-lucide="save" class="w-5 h-5"></i>
                    Save Superset Block
                </button>
            </div>
        </footer>
    </main>

    <script>
        // Initialize Icons
        lucide.createIcons();

        // Interaction Feedback
        document.querySelectorAll('.btn-action, .glass-input').forEach(el => {
            el.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(8);
                }
            });
        });

        // Subtle Page Entry Animation
        document.addEventListener('DOMContentLoaded', () => {
            const card = document.querySelector('.carbon-surface');
            card.style.opacity = '0';
            card.style.transform = 'translateY(30px)';
            
            setTimeout(() => {
                card.style.transition = 'all 0.8s cubic-bezier(0.16, 1, 0.3, 1)';
                card.style.opacity = '1';
                card.style.transform = 'translateY(0)';
            }, 100);
        });
    </script>
</body>
</html>