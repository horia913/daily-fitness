<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Client | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-dark: #0A0A0A;
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
            min-height: 100vh;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Crystalline Kinetic Refraction */
        .crystal-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.01) 100%);
            backdrop-filter: blur(30px) saturate(180%);
            border: 1px solid var(--glass-border);
            border-radius: 32px;
            position: relative;
            overflow: hidden;
        }

        .crystal-card::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(45deg, transparent 40%, rgba(255,255,255,0.05) 50%, transparent 60%);
            pointer-events: none;
            animation: crystal-glint 8s infinite linear;
        }

        @keyframes crystal-glint {
            0% { transform: translateX(-100%) translateY(-100%); }
            100% { transform: translateX(100%) translateY(100%); }
        }

        .input-group {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .input-field {
            width: 100%;
            height: 56px;
            background: rgba(255, 255, 255, 0.03);
            border: 1.5px solid var(--glass-border);
            border-radius: 16px;
            padding: 0 16px;
            color: white;
            font-size: 16px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            outline: none;
        }

        .input-field:focus {
            border-color: var(--secondary);
            background: rgba(59, 130, 246, 0.05);
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.15);
        }

        .input-label {
            display: block;
            font-size: 14px;
            font-weight: 500;
            color: #A3A3A3;
            margin-bottom: 8px;
            margin-left: 4px;
        }

        .btn-success {
            height: 56px;
            background: linear-gradient(135deg, #10B981 0%, #059669 100%);
            box-shadow: 0 4px 16px rgba(16, 185, 129, 0.3);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 16px;
            font-weight: 700;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            width: 100%;
        }

        .btn-success:active { transform: scale(0.96); }
        .btn-success:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(16, 185, 129, 0.4); }

        .btn-secondary {
            height: 56px;
            background: transparent;
            border: 1.5px solid var(--glass-border);
            color: #A3A3A3;
            transition: all 0.2s ease;
            border-radius: 16px;
            font-weight: 600;
        }

        .btn-secondary:hover { background: rgba(255, 255, 255, 0.05); color: white; }

        .type-selector {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }

        .type-option {
            cursor: pointer;
            position: relative;
        }

        .type-option input { display: none; }

        .type-content {
            height: 56px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.03);
            border: 1.5px solid var(--glass-border);
            border-radius: 16px;
            font-weight: 600;
            color: #A3A3A3;
            transition: all 0.3s ease;
        }

        .type-option input:checked + .type-content {
            border-color: var(--accent);
            background: rgba(16, 185, 129, 0.1);
            color: white;
        }

        .sticky-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(20px);
            padding: 16px 24px 32px 24px;
            border-top: 1px solid var(--glass-border);
            z-index: 50;
        }

        .refraction-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: 
                radial-gradient(circle at 80% 20%, rgba(16, 185, 129, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.08) 0%, transparent 40%);
            z-index: -1;
        }

        /* Custom Date Picker Styling */
        input[type="date"]::-webkit-calendar-picker-indicator {
            filter: invert(1);
            opacity: 0.5;
        }

        .loader {
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 0.8s linear infinite;
            display: none;
        }

        @keyframes spin { to { transform: rotate(360deg); } }
    </style>
</head>
<body class="p-6 md:p-12 pb-48">
    <div class="refraction-bg"></div>

    <main class="max-w-2xl mx-auto">
        <!-- Header -->
        <header class="flex items-center gap-4 mb-10">
            <button onclick="history.back()" class="w-12 h-12 flex items-center justify-center rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 transition-all">
                <i data-lucide="chevron-left" class="w-6 h-6"></i>
            </button>
            <div>
                <h1 class="text-3xl font-bold tracking-tight">Add New Client</h1>
                <p class="text-sm text-neutral-400 mono uppercase tracking-wider mt-1">Onboarding Module v2.0</p>
            </div>
        </header>

        <!-- Form Body -->
        <form id="addClientForm" class="space-y-8">
            <div class="crystal-card p-6 md:p-8 space-y-6">
                <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                    <i data-lucide="user" class="w-5 h-5 text-secondary"></i>
                    Personal Information
                </h2>

                <!-- Name Input -->
                <div class="input-group">
                    <label class="input-label">Full Name</label>
                    <input type="text" class="input-field" placeholder="e.g. Marcus Aurelius" required>
                </div>

                <!-- Contact Grid -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="input-group">
                        <label class="input-label">Email Address</label>
                        <input type="email" class="input-field" placeholder="name@example.com" required>
                    </div>
                    <div class="input-group">
                        <label class="input-label">Phone Number (Optional)</label>
                        <input type="tel" class="input-field" placeholder="+1 (555) 000-0000">
                    </div>
                </div>

                <!-- DOB & Gender -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="input-group">
                        <label class="input-label">Date of Birth</label>
                        <input type="date" class="input-field">
                    </div>
                    <div class="input-group">
                        <label class="input-label">Gender</label>
                        <select class="input-field appearance-none">
                            <option value="" disabled selected>Select Gender</option>
                            <option value="male">Male</option>
                            <option value="female">Female</option>
                            <option value="other">Other</option>
                            <option value="private">Prefer not to say</option>
                        </select>
                        <i data-lucide="chevron-down" class="absolute right-4 bottom-4 w-5 h-5 text-neutral-500 pointer-events-none"></i>
                    </div>
                </div>
            </div>

            <div class="crystal-card p-6 md:p-8 space-y-6">
                <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                    <i data-lucide="target" class="w-5 h-5 text-accent"></i>
                    Classification & Goals
                </h2>

                <!-- Client Type -->
                <div class="input-group">
                    <label class="input-label">Client Training Mode</label>
                    <div class="type-selector">
                        <label class="type-option">
                            <input type="radio" name="client_type" value="online" checked>
                            <div class="type-content">Online Coaching</div>
                        </label>
                        <label class="type-option">
                            <input type="radio" name="client_type" value="in-person">
                            <div class="type-content">In-Person</div>
                        </label>
                    </div>
                </div>

                <!-- Goals Textarea -->
                <div class="input-group">
                    <label class="input-label">Primary Goals & Notes</label>
                    <textarea class="input-field h-32 py-4 resize-none" placeholder="Hypertrophy focus, 12-week weight loss target, previous ACL injury..."></textarea>
                </div>
            </div>
        </form>

        <!-- Actions -->
        <div class="sticky-footer">
            <div class="max-w-2xl mx-auto flex flex-col md:flex-row gap-4">
                <button type="button" onclick="history.back()" class="btn-secondary flex-1 order-2 md:order-1">
                    Cancel
                </button>
                <button type="submit" form="addClientForm" id="submitBtn" class="btn-success flex-[2] order-1 md:order-2">
                    <span id="btnText">Create Client</span>
                    <div id="btnLoader" class="loader"></div>
                    <i data-lucide="user-plus" class="w-5 h-5" id="btnIcon"></i>
                </button>
            </div>
        </div>
    </main>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Form Submission Simulation
        const form = document.getElementById('addClientForm');
        const submitBtn = document.getElementById('submitBtn');
        const btnText = document.getElementById('btnText');
        const btnLoader = document.getElementById('btnLoader');
        const btnIcon = document.getElementById('btnIcon');

        form.addEventListener('submit', (e) => {
            e.preventDefault();
            
            // Visual feedback
            submitBtn.disabled = true;
            btnText.textContent = "Processing...";
            btnLoader.style.display = "block";
            btnIcon.style.display = "none";
            submitBtn.style.opacity = "0.8";

            if (window.navigator.vibrate) {
                window.navigator.vibrate([10, 30, 10]);
            }

            // Simulate API Call
            setTimeout(() => {
                btnLoader.style.display = "none";
                btnIcon.style.display = "block";
                btnIcon.setAttribute('data-lucide', 'check');
                lucide.createIcons();
                submitBtn.style.background = "var(--accent)";
                btnText.textContent = "Client Created ✓";
                
                // Transition out
                setTimeout(() => {
                    alert("Client successfully added to your roster!");
                    location.reload();
                }, 1000);
            }, 1800);
        });

        // Add crystal entry animation
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.crystal-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.8s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 150);
            });
        });
    </script>
</body>
</html>