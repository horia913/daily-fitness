<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Templates | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-dark: #0A0A0A;
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
            --tectonic-edge: rgba(255, 255, 255, 0.05);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
            background-image: 
                linear-gradient(var(--tectonic-edge) 1px, transparent 1px),
                linear-gradient(90deg, var(--tectonic-edge) 1px, transparent 1px);
            background-size: 40px 40px;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Crystalline Kinetic Refraction */
        .crystal-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.01) 100%);
            backdrop-filter: blur(20px) saturate(180%);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        .crystal-card:hover {
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-4px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        /* Machined Tectonic Accents */
        .machined-border {
            border-left: 4px solid var(--primary);
        }

        .tectonic-layer {
            position: relative;
            z-index: 10;
        }

        .sticky-search {
            position: sticky;
            top: 0;
            z-index: 50;
            background: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--glass-border);
        }

        .kinetic-shimmer {
            position: relative;
            overflow: hidden;
        }

        .kinetic-shimmer::after {
            content: '';
            position: absolute;
            top: -100%;
            left: -100%;
            width: 300%;
            height: 300%;
            background: linear-gradient(45deg, transparent 45%, rgba(255,255,255,0.08) 50%, transparent 55%);
            animation: shine 8s infinite linear;
            pointer-events: none;
        }

        @keyframes shine {
            0% { transform: translate(-30%, -30%); }
            100% { transform: translate(30%, 30%); }
        }

        .btn-primary {
            height: 48px;
            background: linear-gradient(135deg, #EF4444 0%, #B91C1C 100%);
            box-shadow: 0 4px 16px rgba(239, 68, 68, 0.3);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .btn-primary:active { transform: scale(0.95); }
        .btn-primary:hover { box-shadow: 0 6px 20px rgba(239, 68, 68, 0.5); }

        .btn-fab {
            width: 56px;
            height: 56px;
            border-radius: 18px;
            background: linear-gradient(135deg, #10B981 0%, #059669 100%);
            box-shadow: 0 8px 24px rgba(16, 185, 129, 0.4);
            position: fixed;
            bottom: 24px;
            right: 24px;
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .btn-fab:hover { transform: scale(1.1) rotate(90deg); }

        .search-input {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--glass-border);
            border-radius: 14px;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            background: rgba(255, 255, 255, 0.08);
            border-color: var(--secondary);
            outline: none;
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }

        .badge {
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            padding: 4px 10px;
            border-radius: 6px;
            background: rgba(255,255,255,0.1);
            border: 1px solid rgba(255,255,255,0.1);
        }

        .refraction-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: 
                radial-gradient(circle at 80% 20%, rgba(239, 68, 68, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.08) 0%, transparent 40%);
            z-index: -1;
        }

        .hide-scrollbar::-webkit-scrollbar { display: none; }
        .hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

        /* Custom Dropdown / Quick Action Sheet Simulation */
        .action-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: rgba(20, 20, 20, 0.95);
            backdrop-filter: blur(10px);
            transform: translateY(100%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .crystal-card:hover .action-overlay {
            transform: translateY(0);
        }

        .text-h1 { font-size: 30px; line-height: 1.25; font-weight: 700; }
        .text-h2 { font-size: 20px; line-height: 1.25; font-weight: 600; }
    </style>
</head>
<body class="pb-24">
    <div class="refraction-bg"></div>

    <!-- Sticky Navigation / Search -->
    <nav class="sticky-search px-6 py-4 mb-6">
        <div class="max-w-6xl mx-auto">
            <div class="flex flex-col md:flex-row gap-4 items-center">
                <div class="relative w-full md:flex-1">
                    <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-neutral-500"></i>
                    <input type="text" placeholder="Search workout templates..." class="search-input w-full pl-12 pr-4 h-12 text-body">
                </div>
                <div class="flex gap-2 w-full md:w-auto overflow-x-auto hide-scrollbar">
                    <button class="flex items-center gap-2 px-4 h-12 crystal-card border-white/10 hover:border-white/30 whitespace-nowrap">
                        <i data-lucide="filter" class="w-4 h-4"></i>
                        <span class="text-sm font-medium">Filters</span>
                    </button>
                    <select class="px-4 h-12 crystal-card border-white/10 bg-transparent text-sm font-medium appearance-none cursor-pointer pr-10">
                        <option>Sort: Recently Used</option>
                        <option>Sort: Name A-Z</option>
                        <option>Sort: Popularity</option>
                    </select>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-6">
        <!-- Header -->
        <header class="mb-10 flex justify-between items-end">
            <div>
                <h1 class="text-h1 tracking-tight mb-2">Workout Templates</h1>
                <p class="text-neutral-400 text-sm mono uppercase tracking-widest">Library / 24 Active Protocols</p>
            </div>
            <div class="hidden md:flex gap-3">
                <div class="text-right">
                    <div class="text-[10px] text-neutral-500 uppercase mono">Total Exercises</div>
                    <div class="text-xl font-bold">412</div>
                </div>
                <div class="w-px h-10 bg-white/10"></div>
                <div class="text-right">
                    <div class="text-[10px] text-neutral-500 uppercase mono">Avg. Intensity</div>
                    <div class="text-xl font-bold text-primary">RPE 8.5</div>
                </div>
            </div>
        </header>

        <!-- Filter Pills (Multi-select simulation) -->
        <div class="flex gap-2 mb-8 overflow-x-auto hide-scrollbar pb-2">
            <span class="badge bg-primary/20 border-primary/30 text-primary cursor-pointer">All Body</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Upper Body</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Lower Body</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Push Day</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Pull Day</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Hypertrophy</span>
            <span class="badge hover:bg-white/10 cursor-pointer">Barbell Only</span>
        </div>

        <!-- Template Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
            
            <!-- Card 1 -->
            <div class="crystal-card machined-border group">
                <div class="p-6">
                    <div class="flex justify-between items-start mb-4">
                        <div class="flex gap-2 mb-2">
                            <span class="badge text-secondary border-secondary/30 bg-secondary/10">Hypertrophy</span>
                            <span class="badge text-accent border-accent/30 bg-accent/10">Advanced</span>
                        </div>
                        <button class="p-2 text-neutral-500 hover:text-white transition-colors">
                            <i data-lucide="more-vertical" class="w-5 h-5"></i>
                        </button>
                    </div>
                    <h3 class="text-2xl font-bold mb-4 group-hover:text-primary transition-colors">V-Taper Protocol: Phase A</h3>
                    
                    <div class="grid grid-cols-3 gap-4 mb-6">
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Exercises</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="dumbbell" class="w-3 h-3"></i> 8</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Duration</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="clock" class="w-3 h-3"></i> 65m</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Muscle</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="target" class="w-3 h-3"></i> Back/Sh</span>
                        </div>
                    </div>

                    <div class="flex gap-3">
                        <button class="btn-primary flex-1 rounded-xl flex items-center justify-center gap-2 font-bold text-sm">
                            <i data-lucide="user-plus" class="w-4 h-4"></i> Assign
                        </button>
                        <button class="btn-secondary px-4 rounded-xl flex items-center justify-center transition-all hover:bg-secondary/20">
                            <i data-lucide="edit-3" class="w-4 h-4"></i>
                        </button>
                    </div>
                </div>
                <!-- Quick Action Sheet (Hover/Mobile) -->
                <div class="action-overlay p-4 flex justify-around border-t border-white/10">
                    <button class="flex flex-col items-center gap-1 text-[10px] mono uppercase text-neutral-400 hover:text-white">
                        <i data-lucide="copy" class="w-5 h-5"></i> Duplicate
                    </button>
                    <button class="flex flex-col items-center gap-1 text-[10px] mono uppercase text-neutral-400 hover:text-white">
                        <i data-lucide="share-2" class="w-5 h-5"></i> Share
                    </button>
                    <button class="flex flex-col items-center gap-1 text-[10px] mono uppercase text-red-400 hover:text-red-500">
                        <i data-lucide="trash-2" class="w-5 h-5"></i> Delete
                    </button>
                </div>
            </div>

            <!-- Card 2 -->
            <div class="crystal-card machined-border group" style="border-left-color: var(--secondary);">
                <div class="p-6">
                    <div class="flex justify-between items-start mb-4">
                        <div class="flex gap-2 mb-2">
                            <span class="badge text-secondary border-secondary/30 bg-secondary/10">Strength</span>
                            <span class="badge text-neutral-400 border-white/10 bg-white/5">Intermediate</span>
                        </div>
                        <button class="p-2 text-neutral-500 hover:text-white transition-colors">
                            <i data-lucide="more-vertical" class="w-5 h-5"></i>
                        </button>
                    </div>
                    <h3 class="text-2xl font-bold mb-4 group-hover:text-secondary transition-colors">Full Body Squat Focus</h3>
                    
                    <div class="grid grid-cols-3 gap-4 mb-6">
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Exercises</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="dumbbell" class="w-3 h-3"></i> 5</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Duration</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="clock" class="w-3 h-3"></i> 45m</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Muscle</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="target" class="w-3 h-3"></i> Legs</span>
                        </div>
                    </div>

                    <div class="flex gap-3">
                        <button class="btn-primary flex-1 rounded-xl flex items-center justify-center gap-2 font-bold text-sm" style="background: linear-gradient(135deg, #3B82F6 0%, #1D4ED8 100%); box-shadow: 0 4px 16px rgba(59, 130, 246, 0.3);">
                            <i data-lucide="user-plus" class="w-4 h-4"></i> Assign
                        </button>
                        <button class="btn-secondary px-4 rounded-xl flex items-center justify-center transition-all hover:bg-secondary/20">
                            <i data-lucide="edit-3" class="w-4 h-4"></i>
                        </button>
                    </div>
                </div>
                <div class="action-overlay p-4 flex justify-around border-t border-white/10">
                    <button class="flex flex-col items-center gap-1 text-[10px] mono uppercase text-neutral-400 hover:text-white">
                        <i data-lucide="copy" class="w-5 h-5"></i> Duplicate
                    </button>
                    <button class="flex flex-col items-center gap-1 text-[10px] mono uppercase text-neutral-400 hover:text-white">
                        <i data-lucide="trash-2" class="w-5 h-5"></i> Delete
                    </button>
                </div>
            </div>

            <!-- Card 3 -->
            <div class="crystal-card machined-border group" style="border-left-color: var(--accent);">
                <div class="p-6">
                    <div class="flex justify-between items-start mb-4">
                        <div class="flex gap-2 mb-2">
                            <span class="badge text-accent border-accent/30 bg-accent/10">Endurance</span>
                            <span class="badge text-neutral-400 border-white/10 bg-white/5">Beginner</span>
                        </div>
                        <button class="p-2 text-neutral-500 hover:text-white transition-colors">
                            <i data-lucide="more-vertical" class="w-5 h-5"></i>
                        </button>
                    </div>
                    <h3 class="text-2xl font-bold mb-4 group-hover:text-accent transition-colors">Kettlebell Engine Builder</h3>
                    
                    <div class="grid grid-cols-3 gap-4 mb-6">
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Exercises</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="dumbbell" class="w-3 h-3"></i> 12</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Duration</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="clock" class="w-3 h-3"></i> 30m</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-[10px] text-neutral-500 uppercase mono">Muscle</span>
                            <span class="font-bold flex items-center gap-1.5"><i data-lucide="target" class="w-3 h-3"></i> Total</span>
                        </div>
                    </div>

                    <div class="flex gap-3">
                        <button class="btn-primary flex-1 rounded-xl flex items-center justify-center gap-2 font-bold text-sm" style="background: linear-gradient(135deg, #10B981 0%, #059669 100%); box-shadow: 0 4px 16px rgba(16, 185, 129, 0.3);">
                            <i data-lucide="user-plus" class="w-4 h-4"></i> Assign
                        </button>
                        <button class="btn-secondary px-4 rounded-xl flex items-center justify-center transition-all hover:bg-secondary/20">
                            <i data-lucide="edit-3" class="w-4 h-4"></i>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Card 4 (Ghost/Empty State Preview) -->
            <div class="crystal-card border-dashed border-2 border-white/10 flex flex-col items-center justify-center p-8 opacity-60 hover:opacity-100 transition-opacity cursor-pointer">
                <div class="w-16 h-16 rounded-full bg-white/5 flex items-center justify-center mb-4">
                    <i data-lucide="plus" class="w-8 h-8 text-neutral-400"></i>
                </div>
                <h3 class="text-h2 font-bold mb-1">Create New Template</h3>
                <p class="text-caption text-neutral-500">Design a custom workout protocol</p>
            </div>

        </div>
    </main>

    <!-- Floating Action Button -->
    <button class="btn-fab kinetic-shimmer" aria-label="Create New Template">
        <i data-lucide="plus" class="text-white w-8 h-8"></i>
    </button>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Subtle entry animations
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.crystal-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 80);
            });
        });

        // Haptic Feedback for Primary Actions
        document.querySelectorAll('.btn-primary, .btn-fab').forEach(btn => {
            btn.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(12);
                }
            });
        });
    </script>
</body>
</html>