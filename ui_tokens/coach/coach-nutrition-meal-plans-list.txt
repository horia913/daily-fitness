<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meal Plan Library | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-dark: #0A0A0A;
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        .crystal-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 100%);
            backdrop-filter: blur(20px) saturate(180%);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            position: relative;
            overflow: hidden;
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), border-color 0.3s ease;
        }

        .crystal-card:hover {
            transform: translateY(-4px);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .crystal-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 70%);
            transform: rotate(30deg);
            pointer-events: none;
        }

        .kinetic-shimmer {
            position: relative;
            overflow: hidden;
        }

        .kinetic-shimmer::after {
            content: '';
            position: absolute;
            top: -100%;
            left: -100%;
            width: 300%;
            height: 300%;
            background: linear-gradient(45deg, transparent 45%, rgba(255,255,255,0.08) 50%, transparent 55%);
            animation: shine 8s infinite linear;
        }

        @keyframes shine {
            0% { transform: translate(-30%, -30%); }
            100% { transform: translate(30%, 30%); }
        }

        .btn-action {
            height: 44px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            border-radius: 12px;
            font-weight: 500;
            font-size: 14px;
            transition: all 0.2s ease;
        }

        .btn-action:active { transform: scale(0.95); }

        .btn-fab {
            width: 56px;
            height: 56px;
            background: linear-gradient(135deg, #10B981 0%, #059669 100%);
            box-shadow: 0 8px 32px rgba(16, 185, 129, 0.4);
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: fixed;
            bottom: 32px;
            right: 24px;
            z-index: 50;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .btn-fab:hover {
            transform: scale(1.1) rotate(5deg);
            box-shadow: 0 12px 40px rgba(16, 185, 129, 0.5);
        }

        .search-container {
            position: sticky;
            top: 0;
            z-index: 40;
            padding: 16px 0;
            background: linear-gradient(to bottom, var(--bg-dark) 60%, transparent);
        }

        .search-input {
            background: rgba(255,255,255,0.05);
            border: 1px solid var(--glass-border);
            height: 56px;
            border-radius: 16px;
            padding: 0 52px;
            width: 100%;
            color: white;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            background: rgba(255,255,255,0.08);
            border-color: var(--secondary);
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }

        .refraction-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: 
                radial-gradient(circle at 10% 20%, rgba(59, 130, 246, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 90% 80%, rgba(239, 68, 68, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 50% 50%, rgba(16, 185, 129, 0.04) 0%, transparent 60%);
            z-index: -1;
        }

        .text-h1 { font-size: 30px; line-height: 1.2; font-weight: 700; letter-spacing: -0.02em; }
        .text-h3 { font-size: 18px; font-weight: 700; line-height: 1.3; }

        .tag-badge {
            background: rgba(59, 130, 246, 0.15);
            color: #60A5FA;
            border: 1px solid rgba(59, 130, 246, 0.3);
            padding: 2px 10px;
            border-radius: 99px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .avatar-stack {
            display: flex;
            align-items: center;
        }

        .avatar-stack img {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            border: 2px solid var(--bg-dark);
            margin-left: -8px;
        }

        .avatar-stack img:first-child { margin-left: 0; }

        .macro-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
        }
    </style>
</head>
<body class="p-6 md:p-10 pb-32">
    <div class="refraction-bg"></div>

    <!-- Header Section -->
    <header class="mb-8">
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-4">
            <div>
                <h1 class="text-h1 mb-2">Meal Plan Templates</h1>
                <p class="text-neutral-400 text-sm">Design, manage, and assign kinetic nutrition protocols.</p>
            </div>
            <div class="flex gap-3">
                <button class="btn-action bg-white/5 border border-white/10 px-4 hover:bg-white/10">
                    <i data-lucide="filter" class="w-4 h-4"></i>
                    <span>Filter</span>
                </button>
                <button class="btn-action bg-white/5 border border-white/10 px-4 hover:bg-white/10">
                    <i data-lucide="archive" class="w-4 h-4"></i>
                    <span>Archive</span>
                </button>
            </div>
        </div>
    </header>

    <!-- Sticky Search Bar -->
    <div class="search-container mb-8">
        <div class="relative max-w-2xl">
            <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-neutral-500"></i>
            <input type="text" placeholder="Search by plan name, calorie goal, or tags..." class="search-input text-base">
            <kbd class="absolute right-4 top-1/2 -translate-y-1/2 px-2 py-1 bg-white/5 border border-white/10 rounded text-[10px] text-neutral-500 mono">CMD + K</kbd>
        </div>
    </div>

    <!-- Grid List -->
    <main class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        
        <!-- Meal Plan Card 1 -->
        <div class="crystal-card kinetic-shimmer flex flex-col">
            <div class="p-6 flex-1">
                <div class="flex justify-between items-start mb-4">
                    <span class="tag-badge">14 Days</span>
                    <button class="text-neutral-500 hover:text-white transition-colors">
                        <i data-lucide="more-horizontal" class="w-6 h-6"></i>
                    </button>
                </div>
                <h3 class="text-h3 mb-2">Hypertrophy Fuel 3000</h3>
                <p class="text-sm text-neutral-400 mb-6 leading-relaxed">High-protein protocol optimized for heavy resistance training and recovery cycles.</p>
                
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Daily Target</span>
                        <span class="text-xl font-bold mono text-accent">3,200 <span class="text-xs font-normal">kcal</span></span>
                    </div>
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Complexity</span>
                        <div class="flex gap-1 mt-1">
                            <div class="h-1.5 w-4 rounded-full bg-accent"></div>
                            <div class="h-1.5 w-4 rounded-full bg-accent"></div>
                            <div class="h-1.5 w-4 rounded-full bg-neutral-700"></div>
                        </div>
                    </div>
                </div>

                <div class="flex items-center justify-between mb-2">
                    <div class="avatar-stack">
                        <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=Felix" alt="User">
                        <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=Sarah" alt="User">
                        <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=Mike" alt="User">
                        <div class="w-7 h-7 rounded-full bg-neutral-800 border-2 border-black flex items-center justify-center -ml-2 text-[10px] font-bold">+12</div>
                    </div>
                    <span class="text-[11px] text-neutral-500 mono">ASSIGNED TO 15 CLIENTS</span>
                </div>
            </div>
            
            <div class="p-4 grid grid-cols-4 gap-2 border-t border-white/5 bg-white/5">
                <button class="flex flex-col items-center gap-1 text-neutral-400 hover:text-secondary transition-colors py-2" aria-label="Edit">
                    <i data-lucide="edit-3" class="w-5 h-5"></i>
                    <span class="text-[9px] font-bold uppercase tracking-widest">Edit</span>
                </button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 hover:text-accent transition-colors py-2" aria-label="Assign">
                    <i data-lucide="user-plus" class="w-5 h-5"></i>
                    <span class="text-[9px] font-bold uppercase tracking-widest">Assign</span>
                </button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 hover:text-white transition-colors py-2" aria-label="Duplicate">
                    <i data-lucide="copy" class="w-5 h-5"></i>
                    <span class="text-[9px] font-bold uppercase tracking-widest">Clone</span>
                </button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 hover:text-primary transition-colors py-2" aria-label="Delete">
                    <i data-lucide="trash-2" class="w-5 h-5"></i>
                    <span class="text-[9px] font-bold uppercase tracking-widest">Delete</span>
                </button>
            </div>
        </div>

        <!-- Meal Plan Card 2 (Keto/Low Carb) -->
        <div class="crystal-card kinetic-shimmer flex flex-col" style="border-left: 4px solid var(--secondary);">
            <div class="p-6 flex-1">
                <div class="flex justify-between items-start mb-4">
                    <span class="tag-badge !bg-blue-500/10 !text-blue-400 !border-blue-500/30">7 Days</span>
                    <button class="text-neutral-500 hover:text-white transition-colors">
                        <i data-lucide="more-horizontal" class="w-6 h-6"></i>
                    </button>
                </div>
                <h3 class="text-h3 mb-2">Metabolic Reset (Low Carb)</h3>
                <p class="text-sm text-neutral-400 mb-6 leading-relaxed">Aggressive insulin sensitivity restoration. Focus on healthy fats and fibrous greens.</p>
                
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Daily Target</span>
                        <span class="text-xl font-bold mono text-secondary">1,850 <span class="text-xs font-normal">kcal</span></span>
                    </div>
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Macros</span>
                        <div class="flex items-center gap-2 mt-1">
                            <span class="w-2 h-2 rounded-full bg-primary" title="Protein"></span>
                            <span class="w-2 h-2 rounded-full bg-blue-400" title="Fats"></span>
                            <span class="w-2 h-2 rounded-full bg-neutral-600" title="Carbs"></span>
                        </div>
                    </div>
                </div>

                <div class="flex items-center justify-between mb-2">
                    <div class="avatar-stack">
                        <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=Emma" alt="User">
                        <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=John" alt="User">
                    </div>
                    <span class="text-[11px] text-neutral-500 mono">ASSIGNED TO 2 CLIENTS</span>
                </div>
            </div>
            
            <div class="p-4 grid grid-cols-4 gap-2 border-t border-white/5 bg-white/5">
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-secondary"><i data-lucide="edit-3" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Edit</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-accent"><i data-lucide="user-plus" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Assign</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-white"><i data-lucide="copy" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Clone</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-primary"><i data-lucide="trash-2" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Delete</span></button>
            </div>
        </div>

        <!-- Meal Plan Card 3 (Endurance) -->
        <div class="crystal-card kinetic-shimmer flex flex-col" style="border-left: 4px solid var(--primary);">
            <div class="p-6 flex-1">
                <div class="flex justify-between items-start mb-4">
                    <span class="tag-badge !bg-red-500/10 !text-red-400 !border-red-500/30">21 Days</span>
                    <button class="text-neutral-500 hover:text-white transition-colors">
                        <i data-lucide="more-horizontal" class="w-6 h-6"></i>
                    </button>
                </div>
                <h3 class="text-h3 mb-2">Endurance Ultra-Base</h3>
                <p class="text-sm text-neutral-400 mb-6 leading-relaxed">High-carbohydrate loading strategy for triathletes and marathon-distance runners.</p>
                
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Daily Target</span>
                        <span class="text-xl font-bold mono text-primary">4,200 <span class="text-xs font-normal">kcal</span></span>
                    </div>
                    <div class="bg-white/5 rounded-xl p-3 border border-white/5">
                        <span class="text-[10px] uppercase text-neutral-500 block mb-1 font-bold">Type</span>
                        <span class="text-xs font-semibold mono">GLYCO-MAX</span>
                    </div>
                </div>

                <div class="flex items-center justify-between mb-2">
                    <div class="avatar-stack">
                        <div class="w-7 h-7 rounded-full bg-neutral-800 border-2 border-white/10 flex items-center justify-center text-[10px] font-bold text-neutral-500">0</div>
                    </div>
                    <span class="text-[11px] text-neutral-500 mono">NOT CURRENTLY ASSIGNED</span>
                </div>
            </div>
            
            <div class="p-4 grid grid-cols-4 gap-2 border-t border-white/5 bg-white/5">
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-secondary"><i data-lucide="edit-3" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Edit</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-accent"><i data-lucide="user-plus" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Assign</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-white"><i data-lucide="copy" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Clone</span></button>
                <button class="flex flex-col items-center gap-1 text-neutral-400 py-2 hover:text-primary"><i data-lucide="trash-2" class="w-5 h-5"></i><span class="text-[9px] font-bold uppercase">Delete</span></button>
            </div>
        </div>

    </main>

    <!-- Floating Action Button -->
    <button class="btn-fab group" aria-label="Create New Meal Plan">
        <i data-lucide="plus" class="w-8 h-8 text-white transition-transform group-hover:scale-110"></i>
        <!-- Tooltip for desktop -->
        <span class="absolute right-full mr-4 bg-accent text-white px-3 py-1.5 rounded-lg text-sm font-bold opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap hidden md:block">
            Create Meal Plan
        </span>
    </button>

    <script>
        // Initialize Lucide
        lucide.createIcons();

        // Staggered Entry Animation
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.crystal-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100 + 100);
            });
        });

        // Search Input Focus Shortcut
        document.addEventListener('keydown', (e) => {
            if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
                e.preventDefault();
                document.querySelector('.search-input').focus();
            }
        });

        // Haptic Feedback Simulation
        document.querySelectorAll('button').forEach(btn => {
            btn.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(10);
                }
            });
        });
    </script>
</body>
</html>