<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Progression Rules | Crystalline Kinetic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #EF4444;
            --secondary: #3B82F6;
            --accent: #10B981;
            --bg-obsidian: #050505;
            --glass-core: rgba(10, 10, 10, 0.7);
            --glass-border: rgba(255, 255, 255, 0.08);
            --spectral-glow: rgba(239, 68, 68, 0.15);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-obsidian);
            color: #FFFFFF;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
        }

        .mono { font-family: 'JetBrains Mono', monospace; }

        /* Spectral Obsidian Material */
        .obsidian-card {
            background: var(--glass-core);
            backdrop-filter: blur(40px) saturate(150%);
            border: 1px solid var(--glass-border);
            border-radius: 28px;
            position: relative;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .spectral-pulse {
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: radial-gradient(circle at 50% 0%, var(--spectral-glow), transparent 70%);
            opacity: 0.4;
            pointer-events: none;
            animation: pulse-breathing 8s infinite ease-in-out;
        }

        @keyframes pulse-breathing {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 0.6; transform: scale(1.05); }
        }

        /* Input Optimization */
        .kinetic-input {
            height: 56px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--glass-border);
            border-radius: 14px;
            padding: 0 16px;
            color: white;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            font-size: 16px;
        }

        .kinetic-input:focus {
            outline: none;
            border-color: var(--secondary);
            background: rgba(59, 130, 246, 0.05);
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }

        .input-label {
            font-size: 13px;
            font-weight: 600;
            color: #737373;
            margin-bottom: 6px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Tab System */
        .tab-active {
            color: white;
            position: relative;
        }
        .tab-active::after {
            content: '';
            position: absolute;
            bottom: -16px;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--primary);
            box-shadow: 0 0 15px var(--primary);
            border-radius: 10px;
        }

        /* Buttons */
        .btn-primary {
            height: 56px;
            background: linear-gradient(135deg, #EF4444 0%, #B91C1C 100%);
            box-shadow: 0 8px 24px rgba(239, 68, 68, 0.3);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 12px 32px rgba(239, 68, 68, 0.4); }
        .btn-primary:active { transform: scale(0.96); }

        .btn-secondary {
            height: 56px;
            background: rgba(255,255,255,0.05);
            border: 1px solid var(--glass-border);
            transition: all 0.2s ease;
        }
        .btn-secondary:hover { background: rgba(255,255,255,0.1); }

        /* Day Pill Scroller */
        .day-scroller::-webkit-scrollbar { display: none; }
        
        .day-pill {
            padding: 10px 20px;
            border-radius: 99px;
            background: rgba(255,255,255,0.03);
            border: 1px solid var(--glass-border);
            white-space: nowrap;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .day-pill.active {
            background: var(--secondary);
            border-color: var(--secondary);
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.4);
        }

        .block-type-badge {
            background: rgba(59, 130, 246, 0.15);
            color: var(--secondary);
            border: 1px solid rgba(59, 130, 246, 0.3);
            padding: 4px 12px;
            border-radius: 8px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Accordion Animation */
        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .accordion-item.active .accordion-content {
            max-height: 2000px;
        }
        .accordion-item.active .chevron-icon {
            transform: rotate(180deg);
        }
    </style>
</head>
<body class="p-4 md:p-8 lg:p-12 pb-32">
    <!-- Spectral Background Layers -->
    <div class="fixed inset-0 z-[-1] overflow-hidden">
        <div class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-red-600/10 blur-[120px] rounded-full"></div>
        <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-blue-600/10 blur-[120px] rounded-full"></div>
    </div>

    <main class="max-w-4xl mx-auto">
        <!-- Header & Top Actions -->
        <header class="flex justify-between items-start mb-10">
            <div>
                <nav class="flex items-center gap-2 text-xs text-neutral-500 mb-4 mono uppercase tracking-[0.2em]">
                    <span class="hover:text-white cursor-pointer transition-colors">Programs</span>
                    <i data-lucide="chevron-right" class="w-3 h-3"></i>
                    <span class="text-neutral-300">Progression Rules</span>
                </nav>
                <h1 class="text-3xl md:text-4xl font-bold tracking-tight">Progression Rules</h1>
                <p class="text-neutral-400 mt-2">Fine-tune intensities and volume for the initial phase.</p>
            </div>
            <button class="text-sm font-medium text-neutral-500 hover:text-white transition-colors bg-white/5 px-4 py-2 rounded-lg border border-white/5">
                Skip for Now
            </button>
        </header>

        <!-- Main Tab Navigation -->
        <div class="flex items-center gap-8 border-b border-white/10 mb-10 overflow-x-auto pb-4 day-scroller">
            <button class="text-sm font-semibold text-neutral-500 whitespace-nowrap">1. Basic Info</button>
            <button class="text-sm font-semibold text-neutral-500 whitespace-nowrap">2. Weekly Schedule</button>
            <button class="text-sm font-semibold tab-active whitespace-nowrap">3. Progression Rules</button>
        </div>

        <!-- Week / Day Selector Area -->
        <div class="flex flex-col md:flex-row gap-6 mb-8">
            <div class="md:w-1/3">
                <label class="input-label block">Phase Selection</label>
                <div class="kinetic-input flex items-center justify-between bg-white/5 opacity-60 cursor-not-allowed">
                    <span class="font-bold">Week 1 (Locked)</span>
                    <i data-lucide="lock" class="w-4 h-4 text-neutral-500"></i>
                </div>
            </div>
            <div class="md:w-2/3">
                <label class="input-label block">Workout Day</label>
                <div class="flex gap-3 overflow-x-auto day-scroller py-1">
                    <div class="day-pill active font-bold text-sm">Mon: Upper A</div>
                    <div class="day-pill font-bold text-sm text-neutral-400">Wed: Lower A</div>
                    <div class="day-pill font-bold text-sm text-neutral-400">Fri: Upper B</div>
                    <div class="day-pill font-bold text-sm text-neutral-400">Sat: Lower B</div>
                </div>
            </div>
        </div>

        <!-- Rules Editor (Accordions) -->
        <div class="space-y-4">
            <!-- Block 1 -->
            <div class="obsidian-card accordion-item active p-6 md:p-8">
                <div class="spectral-pulse"></div>
                <div class="flex justify-between items-center cursor-pointer mb-6" onclick="toggleAccordion(this)">
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-xl bg-red-500/20 flex items-center justify-center text-red-500 border border-red-500/30">
                            <span class="font-bold text-lg">1</span>
                        </div>
                        <h3 class="text-xl font-bold tracking-tight">Primary Pulls</h3>
                    </div>
                    <div class="flex items-center gap-4">
                        <span class="block-type-badge">Straight Sets</span>
                        <i data-lucide="chevron-down" class="w-6 h-6 text-neutral-500 transition-transform chevron-icon"></i>
                    </div>
                </div>

                <div class="accordion-content">
                    <!-- Exercise Entry 1 -->
                    <div class="space-y-8 pt-4 border-t border-white/5">
                        <div class="flex flex-col lg:flex-row justify-between lg:items-center gap-4">
                            <div>
                                <h4 class="text-lg font-semibold text-white">Weighted Pull-Ups</h4>
                                <p class="text-xs text-neutral-500 mono uppercase mt-1">Target: Lats, Biceps</p>
                            </div>
                            <button class="flex items-center gap-2 text-xs font-bold text-neutral-400 hover:text-white transition-colors bg-white/5 px-3 py-2 rounded-lg border border-white/5">
                                <i data-lucide="refresh-cw" class="w-3.5 h-3.5"></i> REPLACE EXERCISE
                            </button>
                        </div>

                        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4">
                            <div class="flex flex-col">
                                <label class="input-label">Sets</label>
                                <input type="number" class="kinetic-input" value="3">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Reps</label>
                                <input type="text" class="kinetic-input" value="8-10">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Weight</label>
                                <input type="text" class="kinetic-input" placeholder="0 kg">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Rest (s)</label>
                                <input type="number" class="kinetic-input" value="120">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Tempo</label>
                                <input type="text" class="kinetic-input" value="3111">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">RIR</label>
                                <input type="number" class="kinetic-input" value="2">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Notes</label>
                                <button class="kinetic-input flex items-center justify-center text-neutral-500 hover:text-white">
                                    <i data-lucide="message-square" class="w-5 h-5"></i>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Exercise Entry 2 -->
                    <div class="space-y-8 mt-12 pt-8 border-t border-white/5">
                        <div class="flex flex-col lg:flex-row justify-between lg:items-center gap-4">
                            <div>
                                <h4 class="text-lg font-semibold text-white">Seated Cable Row</h4>
                                <p class="text-xs text-neutral-500 mono uppercase mt-1">Target: Mid-Back, Rhomboids</p>
                            </div>
                            <button class="flex items-center gap-2 text-xs font-bold text-neutral-400 hover:text-white transition-colors bg-white/5 px-3 py-2 rounded-lg border border-white/5">
                                <i data-lucide="refresh-cw" class="w-3.5 h-3.5"></i> REPLACE EXERCISE
                            </button>
                        </div>

                        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4">
                            <div class="flex flex-col">
                                <label class="input-label">Sets</label>
                                <input type="number" class="kinetic-input" value="3">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Reps</label>
                                <input type="text" class="kinetic-input" value="12-15">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Weight</label>
                                <input type="text" class="kinetic-input" value="65kg">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Rest (s)</label>
                                <input type="number" class="kinetic-input" value="90">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Tempo</label>
                                <input type="text" class="kinetic-input" value="2011">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">RIR</label>
                                <input type="number" class="kinetic-input" value="1">
                            </div>
                            <div class="flex flex-col">
                                <label class="input-label">Notes</label>
                                <button class="kinetic-input flex items-center justify-center text-neutral-500 hover:text-white">
                                    <i data-lucide="message-square" class="w-5 h-5"></i>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Replace Entire Workout Block -->
                    <div class="mt-10 flex justify-center">
                        <button class="flex items-center gap-2 px-6 py-3 rounded-xl border-2 border-dashed border-white/10 text-neutral-500 hover:border-red-500/50 hover:text-white transition-all text-sm font-bold uppercase tracking-wider">
                            <i data-lucide="zap" class="w-4 h-4"></i> Swap Entire Block
                        </button>
                    </div>
                </div>
            </div>

            <!-- Block 2 (Collapsed) -->
            <div class="obsidian-card accordion-item p-6 md:p-8">
                <div class="flex justify-between items-center cursor-pointer" onclick="toggleAccordion(this)">
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-xl bg-blue-500/20 flex items-center justify-center text-blue-500 border border-blue-500/30">
                            <span class="font-bold text-lg">2</span>
                        </div>
                        <h3 class="text-xl font-bold tracking-tight">Bicep Isolation</h3>
                    </div>
                    <div class="flex items-center gap-4">
                        <span class="block-type-badge !bg-amber-500/15 !text-amber-500 !border-amber-500/30">Drop Set</span>
                        <i data-lucide="chevron-down" class="w-6 h-6 text-neutral-500 transition-transform chevron-icon"></i>
                    </div>
                </div>
                <div class="accordion-content">
                    <p class="py-8 text-neutral-500 text-center italic">Expansion settings for drop-set volume tracking...</p>
                </div>
            </div>
        </div>

        <!-- Sticky Bottom Navigation -->
        <nav class="fixed bottom-0 left-0 right-0 p-4 md:p-6 bg-black/60 backdrop-blur-2xl border-t border-white/10 z-[100]">
            <div class="max-w-4xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4">
                <button class="flex items-center justify-center gap-3 w-full md:w-auto px-10 btn-secondary rounded-2xl font-bold text-neutral-300">
                    <i data-lucide="arrow-left" class="w-5 h-5"></i>
                    Back to Schedule
                </button>
                <button class="flex items-center justify-center gap-3 w-full md:px-12 btn-primary rounded-2xl font-bold text-lg tracking-tight">
                    Save & Finish Program
                    <i data-lucide="check-circle" class="w-6 h-6"></i>
                </button>
            </div>
        </nav>
    </main>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Accordion Toggle
        function toggleAccordion(header) {
            const item = header.parentElement;
            const isActive = item.classList.contains('active');
            
            // Optional: Close all others
            // document.querySelectorAll('.accordion-item').forEach(el => el.classList.remove('active'));
            
            if (isActive) {
                item.classList.remove('active');
            } else {
                item.classList.add('active');
            }
        }

        // Add subtle entry animations
        document.addEventListener('DOMContentLoaded', () => {
            const cards = document.querySelectorAll('.obsidian-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, 100 + (index * 100));
            });
        });

        // Haptic Simulation
        document.querySelectorAll('button, .day-pill').forEach(el => {
            el.addEventListener('click', () => {
                if (window.navigator.vibrate) {
                    window.navigator.vibrate(10);
                }
            });
        });
    </script>
</body>
</html>